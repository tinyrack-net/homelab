apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: juicefs-mount-critical
value: 1000000000
globalDefault: false
preemptionPolicy: Never
description: JuiceFS mount pods remain important without preempting existing workloads.
