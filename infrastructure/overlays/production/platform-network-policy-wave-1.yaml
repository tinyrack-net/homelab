apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: platform-network-policy-wave-1
  namespace: flux-system
spec:
  interval: 15m
  path: ./infrastructure/base/platform-network-policy-wave-1
  # Namespace activation resources must never be pruned.
  prune: false
  timeout: 5m
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: platform-network-security
    - name: traefik
    - name: traefik-external
    - name: cloudflared-deployment
