apiVersion: apps/v1
kind: Deployment
metadata:
  name: litellm-deployment
  namespace: litellm-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: litellm
  template:
    metadata:
      annotations:
        litellm.winetree94.com/config-revision: models-20260620-025653
      labels:
        app: litellm
    spec:
      containers:
        - name: litellm-container
          image: ghcr.io/berriai/litellm-database:v1.89.1
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 4000
              protocol: TCP
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          env:
            - name: DATABASE_USER
              valueFrom:
                secretKeyRef:
                  name: litellm-database-user-secret
                  key: username
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: litellm-database-user-secret
                  key: password
            - name: DATABASE_URL
              value: postgresql://$(DATABASE_USER):$(DATABASE_PASSWORD)@litellm-database-cluster-rw.litellm-system.svc.cluster.local:5432/litellm
            - name: LITELLM_MODE
              value: PRODUCTION
            - name: LITELLM_LOG
              value: ERROR
            - name: LITELLM_LOCAL_MODEL_COST_MAP
              value: "True"
            - name: STORE_MODEL_IN_DB
              value: "False"
            - name: SEARXNG_API_BASE
              value: "https://search.winetree94.com"
            - name: GENERIC_CLIENT_ID
              value: litellm-admin-ui
            - name: GENERIC_AUTHORIZATION_ENDPOINT
              value: https://auth.winetree94.com/oauth/authorize
            - name: GENERIC_TOKEN_ENDPOINT
              value: https://auth.winetree94.com/oauth/token
            - name: GENERIC_USERINFO_ENDPOINT
              value: https://auth.winetree94.com/oauth/userinfo
            - name: GENERIC_SCOPE
              value: "openid email profile"
            - name: GENERIC_CLIENT_USE_PKCE
              value: "true"
            - name: GENERIC_USER_ID_ATTRIBUTE
              value: sub
            - name: GENERIC_USER_DISPLAY_NAME_ATTRIBUTE
              value: name
            - name: GENERIC_USER_EMAIL_ATTRIBUTE
              value: email
            - name: PROXY_BASE_URL
              value: https://llm.winetree94.com
          envFrom:
            - secretRef:
                name: litellm-secret
            - secretRef:
                name: litellm-database-secret
          args:
            - "--config"
            - "/app/proxy_config.yaml"
          volumeMounts:
            - name: config-volume
              mountPath: /app/proxy_config.yaml
              subPath: config.yaml
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health/liveliness
              port: 4000
            initialDelaySeconds: 10
            successThreshold: 1
            failureThreshold: 10
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: 4000
            initialDelaySeconds: 10
            successThreshold: 1
            failureThreshold: 10
            timeoutSeconds: 10
      volumes:
        - name: config-volume
          configMap:
            name: litellm-config
