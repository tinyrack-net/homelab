apiVersion: v1
kind: Service
metadata:
  name: opencodex
  namespace: opencodex-system
spec:
  selector:
    app.kubernetes.io/name: opencodex
  ports:
    - {name: data, port: 10100, targetPort: data}
    - {name: management, port: 10101, targetPort: management}
