apiVersion: apps/v1
kind: Deployment
metadata:
  name: proxer
  namespace: proxer-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: proxer
  template:
    metadata:
      labels:
        app: proxer
    spec:
      containers:
        - name: proxer
          image: ghcr.io/tinyrack-net/proxer:latest@sha256:c9a1ea4ffcd39ab828e828f6c057506cdc8f6b400f089a39d7e10c96a79bb5ea # {"$imagepolicy": "flux-system:proxer"}
          imagePullPolicy: Always
          command:
            - proxer
          args:
            - server
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: PROXER_LISTEN
              value: 0.0.0.0:8080
            - name: PROXER_DOMAIN
              value: proxy.intranet.winetree94.com
            - name: PROXER_TRUSTED_PROXIES
              value: private,loopback
            - name: PROXER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: proxer-token
                  key: PROXER_TOKEN
          livenessProbe:
            httpGet:
              path: /__proxer__/health/live
              port: http
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /__proxer__/health/ready
              port: http
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
      securityContext:
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
