apiVersion: v1
kind: Service
metadata:
  name: ghost-service
  namespace: ghost-system
spec:
  selector:
    app: ghost
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 2368
  type: ClusterIP
