apiVersion: v1
kind: Service
metadata:
  name: cliproxyapi-service
  namespace: cliproxyapi-system
spec:
  selector:
    app: cliproxyapi
  ports:
    - name: http
      port: 8317
      targetPort: http
      protocol: TCP
