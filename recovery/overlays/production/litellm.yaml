apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: recovery-litellm-database
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/base/litellm
  prune: true
  wait: true
  timeout: 60m
  sourceRef:
    kind: GitRepository
    name: flux-system
  patches:
    - target:
        group: postgresql.cnpg.io
        kind: Cluster
        name: litellm-database-cluster
      patch: |-
        - op: replace
          path: /spec/bootstrap
          value:
            recovery:
              source: litellm-database-recovery-source
    - target:
        group: apps
        kind: Deployment
        name: litellm-deployment
      patch: |-
        - op: add
          path: /spec/replicas
          value: 0
    - target:
        group: traefik.io
        kind: IngressRoute
        name: litellm-ingress-route
      patch: |-
        apiVersion: traefik.io/v1alpha1
        kind: IngressRoute
        metadata:
          name: litellm-ingress-route
          namespace: litellm-system
        $patch: delete
  dependsOn:
    - name: cloudnative-pg
    - name: configs
